<nav class="navbar navbar-expand-lg navbar-light bg-primary p-2">
	<a class="navbar-brand text-light" href="/">Home</a>
	<a class="navbar-brand text-light" href="/category/">Categories</a>
	<a class="navbar-brand text-light" href="/program/">Programs</a>
	<a class="navbar-brand text-light" href="/episode/">Episodes</a>
	{% if app.user %}
		<span class="navbar-brand text-light">{{ app.user.email }}</span>
		<a class="navbar-brand text-light" href="{{ path('app_logout') }}">Se déconnecter</a>
	{% else %}
		<a class="navbar-brand text-light" href="{{ path('login') }}">Se connecter</a>
	{% endif %}
</nav>
